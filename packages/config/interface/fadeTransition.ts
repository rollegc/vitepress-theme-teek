export interface FadeTransition {
  /**
   * 是否开启首页文章列表动画
   *
   * @default false
   */
  post?: boolean;
  /**
   * 是否开启首页卡片列表动画
   *
   * @default false
   */
  card?: boolean;
  /**
   * 是否开启归档页动画
   *
   * @default false
   */
  archives?: boolean;
}
